Record types are created using type variables, and for every finite set {l1, ..., ln}, an n-ary constructor is defined to build the record type l1(a1), ..., ln(an) from types a1, ..., an. There are two types of typing judgments that correspond to the two syntactic classes.