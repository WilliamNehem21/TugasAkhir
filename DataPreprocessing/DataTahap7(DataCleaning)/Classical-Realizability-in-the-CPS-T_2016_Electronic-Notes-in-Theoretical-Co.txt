are generated from type variables and for each finite set{l1,..., ln} an n-ary constructor which forms the record type l1(a1),..., ln(an) out of types a1,..., an. there are two kinds of typing judgments corresponding to the two syntactic classes:



