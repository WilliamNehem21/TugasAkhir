this makes dom(f) an open subset of cantor space and f|dom(f) an open continuous mapping of dom(f) into cantor space. when dom(f)= cantor space we say that f is total. so we have two cartesian monoids.



